/* Optional pre-seed for settings. Safe to commit — put NO secrets here.
   Your API key belongs in the app's ⚙ Settings (saved to this browser's
   localStorage, never to git). You *can* drop a key below for convenience,
   but then don't commit/deploy this file with it. */
window.FITCHECK_CONFIG = {
  // model: 'gemini-3.1-flash-image',   // or 'gemini-3-pro-image'
  // imageSize: '1K',                   // or '2K'
  // apiKey: 'AIza...',                 // convenience only — Settings is preferred
};
